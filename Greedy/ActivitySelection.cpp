/*
 *
 *
 * Going for every case will take exponential time complexity, while we can optimize this using DP
 *
 * Here as we have to make choice among all, so we sort by finish time and then select the smallest one
 * That is select the activity which finishes first, now how will we know that we have to select by finishing time or starting time 
 */


