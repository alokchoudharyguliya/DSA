/*
 * Given an array and a sum k, we need to print the length of the longest subarray that sums to k. 
 * NOTE-We don't have to print the subarray rather only the length
 *
 *
 * Naive Approach - Find the all the subarray sums, three loops, first two to iterate over all the subarrays and another loop to find the sum of subarray
 *
 *
 */
#include<bits/stdc++.h>
using namespace std;
int main(){
	return 0;
}
