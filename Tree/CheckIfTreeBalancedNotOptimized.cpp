#include <bits/stdc++.h>
using namespace std;
struct Node
{
    int data;
    Node *left;
    Node *right;
    Node(int val) : data(val), left(nullptr), right(nullptr) {}
};
bool isBalanced(Node*root){
    if(!root)return true;

    // we have a balance factor -1 
    int leftBalanceFactor;
    int rightBalanceFactor;
    
}
int main()
{

}