#include<bits/stdc++.h>
using namespace std;
int Atoi(string s){
    if(s.empty())return 0;

    const long long MAX_INT=INT_MAX;
    const long long MIN_INT=INT_MIN;

    int n=s.length();
    while(i!=n){

    }
}
int main(){
    string s;
    cin>>s;
    cout<<Atoi(s);
}